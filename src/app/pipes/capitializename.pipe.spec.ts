import { CapitializenamePipe } from './capitializename.pipe';

describe('CapitializenamePipe', () => {
  it('create an instance', () => {
    const pipe = new CapitializenamePipe();
    expect(pipe).toBeTruthy();
  });
});
